// jcenter() shut down in 2021, so the original 2017 build cannot resolve
// anything. Modernised to AGP 8 / Gradle 8. No game code is touched.
plugins {
    id 'com.android.application' version '8.3.2' apply false
}
